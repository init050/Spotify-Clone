# Django Core
SECRET_KEY='your-secret-key'
DEBUG=True

# Database (PostgreSQL)
POSTGRES_DB='spotify_clone'
POSTGRES_USER='user'
POSTGRES_PASSWORD='password'
POSTGRES_HOST='db'
POSTGRES_PORT=5432

# Celery (Redis)
CELERY_BROKER_URL='redis://redis:6379/0'
CELERY_RESULT_BACKEND='redis://redis:6379/0'

# Playlists App
PLAYLISTS_MAX_TRACKS=5000
PLAYLISTS_GAP=1000
PLAYLISTS_SHARE_TOKEN_TTL=86400
PLAYLISTS_ALLOW_DUPLICATES_DEFAULT=True
PLAYLISTS_COLLAGE_SIZE=400

# Cache (Redis)
REDIS_CACHE_URL='redis://redis:6379/1'

# Email
EMAIL_BACKEND='django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST='smtp.example.com'
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER='user@example.com'
EMAIL_HOST_PASSWORD='password'
DEFAULT_FROM_EMAIL='noreply@spotifyclone.com'

# JWT Settings
ACCESS_TOKEN_LIFETIME_MINUTES=5
REFRESH_TOKEN_LIFETIME_DAYS=7

# S3 Storage (for MinIO in dev)
AWS_ACCESS_KEY_ID='minioadmin'
AWS_SECRET_ACCESS_KEY='minioadmin'
AWS_STORAGE_BUCKET_NAME='spotify-clone-media'
AWS_S3_ENDPOINT_URL='http://minio:9000'

# Catalog Settings
CATALOG_HLS_VARIANTS='64,128,256' # in kbps

# Streaming Settings
STREAMING_STORAGE_BUCKET='spotify-clone-media' # Can be the same as AWS_STORAGE_BUCKET_NAME
STREAMING_MAX_AUDIO_MB=50
STREAMING_ALLOWED_MIME='audio/mpeg,audio/mp4,audio/flac,audio/wav'
STREAMING_HLS_BITRATES='64,128,256' # in kbps, can be same as CATALOG_HLS_VARIANTS

# Transcoding Tools (optional, if not in system PATH)
FFMPEG_PATH=''
FFPROBE_PATH=''
